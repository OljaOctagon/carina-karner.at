{
  "private": true,
  "scripts": {
    "parse-bib": "node scripts/parse-bib.mjs",
    "build": "npm run parse-bib && nuxt build",
    "dev": "node scripts/parse-bib.mjs && nuxt dev --host 0.0.0.0",
    "generate": "npm run parse-bib && nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "@nuxt/content": "^2.13.2",
    "@nuxt/image": "^1.7.0",
    "@retorquere/bibtex-parser": "^9.0.21",
    "@types/node": "^18.19.44",
    "nuxt": "^3.12.4",
    "sass-embedded": "^1.77.8"
  }
}
